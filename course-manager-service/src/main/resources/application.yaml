server:
  port: 8090
  servlet:
    context-path: /api/course-manager

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/course
    username: postgres
    password: psql123
  jpa:
    hibernate:
      ddl-auto: update