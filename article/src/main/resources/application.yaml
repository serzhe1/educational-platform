server:
  port: 8094
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/serega
    username: serega
    password: serega
  data:
    web:
      pageable:
        one-indexed-parameters: true
        default-page-size: 10
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  main:
    allow-circular-references: true
  jpa:
    database: POSTGRESQL
    show-sql: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
---
spring:
  config:
    activate:
      on-profile: deb
logging:
  level:
    root: DEBUG
